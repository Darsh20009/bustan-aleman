# إعداد الاتصال بقاعدة بيانات AWS RDS

## الحالة الحالية
✅ تم تكوين الاتصال بقاعدة بيانات AWS RDS  
⚠️ الاتصال معطل حالياً بسبب قيود Security Group في AWS

## المشكلة
قاعدة بيانات AWS RDS غير قابلة للوصول من Replit لأن Security Group لا يسمح بالاتصالات من عناوين IP الخاصة بـ Replit.

## الحل المطلوب

### الخطوة 1: تحديث AWS Security Group

1. افتح AWS Console واذهب إلى RDS
2. اختر قاعدة البيانات: `bustan` في منطقة `eu-north-1`
3. انقر على Security Group المرتبطة
4. أضف قاعدة Inbound جديدة:
   - **Type**: PostgreSQL
   - **Protocol**: TCP
   - **Port**: 5432
   - **Source**: `0.0.0.0/0` (للسماح بجميع الاتصالات) أو IP addresses محددة من Replit

⚠️ **ملاحظة أمنية**: استخدام `0.0.0.0/0` يسمح بالوصول من أي مكان. للأمان الأفضل، استخدم عناوين IP محددة فقط.

### الخطوة 2: التحقق من إعدادات VPC

تأكد من أن قاعدة البيانات:
- موجودة في Subnet عام (Public Subnet)
- أو لديها NAT Gateway للوصول من الخارج
- خيار "Publicly Accessible" مفعّل

### الخطوة 3: تفعيل الاتصال بـ AWS RDS

بعد إصلاح Security Group، قم بإضافة متغير البيئة التالي إلى Replit Secrets:

- **المفتاح (Key)**: `ENABLE_AWS_RDS`
- **القيمة (Value)**: `true`

هذا سيُفعل الاتصال بـ AWS RDS تلقائياً، ومع ذلك إذا فشل الاتصال، سيتم الرجوع تلقائياً إلى القاعدة المحلية.

### الخطوة 4: إنشاء Schema في قاعدة البيانات

بعد نجاح الاتصال، قم بتشغيل:

```bash
npm run db:push
```

هذا سيقوم بإنشاء جميع الجداول في قاعدة بيانات AWS.

## معلومات الاتصال المحفوظة

تم حفظ بيانات الاتصال التالية بشكل آمن في Replit Secrets:
- ✅ AWS_DB_HOST: `bustan.czu88we8syd5.eu-north-1.rds.amazonaws.com`
- ✅ AWS_DB_PORT: `5432`
- ✅ AWS_DB_NAME: (محفوظ)
- ✅ AWS_DB_USER: (محفوظ)
- ✅ AWS_DB_PASSWORD: (محفوظ)

## اختبار الاتصال

بعد تطبيق التغييرات، أعد تشغيل التطبيق وابحث عن هذه الرسالة في السجلات:

```
✅ Database connection initialized (AWS RDS)
✅ Successfully connected to AWS RDS
```

إذا ظهرت هذه الرسائل، فإن الاتصال ناجح!

## ملاحظة حول نظام حصتي

كما طلبت، نظام حصتي الحالي يستخدم القاعدة المحلية وسيستمر في استخدامها. عند تفعيل AWS RDS، سيتم استخدام قاعدة البيانات الجديدة للمنصة الرئيسية فقط.
